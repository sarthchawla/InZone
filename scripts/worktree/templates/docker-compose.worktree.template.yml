# Auto-generated by worktree setup
# Worktree: {{WORKTREE_ID}}
# DO NOT EDIT - regenerate with /worktree command

services:
  app:
    container_name: inzone-wt-{{WORKTREE_ID}}
    environment:
      - VITE_DEV_PORT={{FRONTEND_PORT}}
      - API_PORT={{BACKEND_PORT}}
      - DATABASE_URL=postgresql://inzone:inzone_dev@host.docker.internal:{{DATABASE_PORT}}/inzone?schema=public
    ports:
      - "{{FRONTEND_PORT}}:{{FRONTEND_PORT}}"
      - "{{BACKEND_PORT}}:{{BACKEND_PORT}}"
    extra_hosts:
      - "host.docker.internal:host-gateway"

# NOTE: Database runs on HOST at port {{DATABASE_PORT}}
# Container name: inzone-db-wt-{{WORKTREE_ID}}
# Started by worktree setup, not by this compose file
